<p><%= link_to "Return Home", root_path %> <%= link_to "New Product", new_product_path %></p>
<h1>Mes Produits </h1>
<% @products.each do |product| %>
  <p>
    <%=link_to product.name, product_path(product) %> (<%=product.category%>)
  </p>
<%end%>

